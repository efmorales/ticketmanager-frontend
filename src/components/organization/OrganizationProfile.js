const OrganizationProfile = () => {
  const styles = {
    padding: "1em 4em",
  };
  return (
    <div className="" style={styles}>
      <h1>This is the Organization's Profile Page!</h1>
      <p>The main dashboad for the Organization can go here.</p>
    </div>
  );
};

export default OrganizationProfile;
